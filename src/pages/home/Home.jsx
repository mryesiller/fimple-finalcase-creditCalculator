import React, { Fragment } from "react"
import { Loading, Error, Form, InfoSection, Table } from "../../components"

const Home = () => {
  return (
    <Fragment>
      <Form />
      <InfoSection />
      <Table />
    </Fragment>
  )
}

export default Home
